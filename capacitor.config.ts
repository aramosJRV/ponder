import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.jrvsolutions.promptings",
  appName: "Promptings",
  webDir: "dist",
};

export default config;
