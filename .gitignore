node_modules/
.env
data/*
dist/
.DS_Store
supabase/.temp/
supabase/.branches/
ios/App/Pods/
android/.gradle/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/build/
android/captures/
android/local.properties
*.iml

# Local Netlify folder
.netlify
deploy-*.zip

# Secrets — .env alone is not enough; backups and per-env variants leak the
# service role key. Keep this broad.
.env.*
!.env.example
.env.bak*

# Build scratch / caches
.tmpstage/
__pycache__/
*.py[cod]
vite.config.ts.timestamp-*.mjs

_presync_backups/
store-assets/
testing/

# the pericope table is the one unreproducible input in data/ — keep it
!data/pericopes.csv

# Android Studio
android/.idea/
